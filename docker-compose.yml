services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "7000:7000"
    volumes:
      - app-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production

volumes:
  app-data:
